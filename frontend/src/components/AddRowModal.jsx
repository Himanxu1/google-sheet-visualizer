import Modal from 'react-modal';

// eslint-disable-next-line react/prop-types
const AddRowModal = ({ isOpen, closeModal, handleSubmit, handleChange, formData }) => {
  return (
    
  );
};

export default AddRowModal;
